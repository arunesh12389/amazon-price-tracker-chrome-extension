services:
  - type: web
    name: price-tracker-backend
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PIP_NO_CACHE_DIR
        value: "1"